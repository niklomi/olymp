@import '{}/client/style/stylus.import';

.poloska
    padding: 2rem 0.5rem;
    background: $main-color;
    text-align: center;
    p
        color: #FFF;
        font-weight:600
        font-size:18px
        display: inline-block;
        padding: 0 1rem 1rem 0;
