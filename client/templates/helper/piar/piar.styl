@import '{}/client/style/stylus.import';

.rating-table
    tr
        .piar
            background: $main-color
            border-radius: 4px
            padding: 2rem 1rem
            p
                display: inline-block
                color: #fff
                font-weight: 600
                padding: 0 1rem 1rem 0;
                font-size: 18px
